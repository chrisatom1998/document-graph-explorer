/** Shared UI event name constants — kept in a tiny module so consumers don't
 *  eagerly import heavier components just to reference an event string. */
export const FIRST_RUN_GUIDE_REOPEN_EVENT = 'knowledge-nebula-reopen-first-run-guide';
